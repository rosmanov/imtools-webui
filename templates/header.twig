<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ common.project_name }} {% if page.name %} / {{ page.name }}{% endif %}</title>
        <link rel="stylesheet" href="/system/css/main.css" title="" type="test/css" />
    </head>
    <body>
        <div class="wrapper">
            <div class="header">
                <table width="100%" cellpadding="0" cellspacing="0">
                    <tr>
                        <td class="title"><a href="/">ImTools Web UI</a></td>
                        <td class="navigation">{% include 'main-menu.twig' %}</td>

                    </tr>
                </table>
            </div>
            <div class="content">
                <h1>{{ page.name|default('Unknown page') }}</h1>

